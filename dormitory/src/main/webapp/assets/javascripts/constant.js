const HTTP_SERVER = 'http://127.0.0.1:8080';

const STUDENT_LOGIN_URL = `${HTTP_SERVER}/student/login`;

const STUDENT_CHANGE_PASSWORD_URL = `${HTTP_SERVER}/student/change-password`;

const STUDENT_REGISTER_URL = `${HTTP_SERVER}/student/register`;

const  STUDENT__EXPRESSES_URL=`${HTTP_SERVER}/student/expresses`;

const  STUDENT_REPAIRS_URL=`${HTTP_SERVER}/student/repairs`;

const  STUDENT_LEAVE_SCHOOLS_URL=`${HTTP_SERVER}/student/leave-schools`;

const STUDENTS_URL=`${HTTP_SERVER}/students`;

const ADMIN_LOGIN_URL = `${HTTP_SERVER}/admin/login`;

const ADMIN_CHANGE_PASSWORD_URL = `${HTTP_SERVER}/admin/change-password`;

const  ADMIN_STUDENTS_URL=`${HTTP_SERVER}/admin/students`;

const  ADMIN_EXPRESSES_URL=`${HTTP_SERVER}/admin/expresses`;

const  ADMIN_REPAIRS_URL=`${HTTP_SERVER}/admin/repairs`;

const  ADMIN_LATE_BACKS_URL=`${HTTP_SERVER}/admin/late-backs`;

const  ADMIN_LEAVE_SCHOOLS_URL=`${HTTP_SERVER}/admin/leave-schools`;

const SUCCESS = 0;
